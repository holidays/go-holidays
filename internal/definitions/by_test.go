// Code generated by gen-holidays from definitions/by.yaml@v9.0.0. DO NOT EDIT.

package definitions_test

import (
	"fmt"

	holidays "github.com/holidays/go-holidays"

	. "github.com/onsi/ginkgo/v2"
	. "github.com/onsi/gomega"
)

var _ = Describe("by", func() {
	DescribeTable("000_НовыГод", func(s string) {
		opts := holidays.Options{Regions: []string{"by"}}
		d, err := parseFlex(s)
		Expect(err).NotTo(HaveOccurred())
		hols, err := holidays.On(d, opts)
		Expect(err).NotTo(HaveOccurred())
		Expect(hasNamed(hols, "Новы год")).To(BeTrue(), fmt.Sprintf("On(%s, %v): want holiday %q, got %v", s, opts.Regions, "Новы год", hols))
	},
		Entry("2017-01-01", "2017-01-01"),
		Entry("2026-01-01", "2026-01-01"),
	)

	DescribeTable("001_НовыГод", func(s string) {
		opts := holidays.Options{Regions: []string{"by"}}
		d, err := parseFlex(s)
		Expect(err).NotTo(HaveOccurred())
		hols, err := holidays.On(d, opts)
		Expect(err).NotTo(HaveOccurred())
		Expect(hasNamed(hols, "Новы год")).To(BeTrue(), fmt.Sprintf("On(%s, %v): want holiday %q, got %v", s, opts.Regions, "Новы год", hols))
	},
		Entry("2017-01-02", "2017-01-02"),
		Entry("2026-01-02", "2026-01-02"),
	)

	DescribeTable("002_КалядыПраваслаўныя", func(s string) {
		opts := holidays.Options{Regions: []string{"by"}}
		d, err := parseFlex(s)
		Expect(err).NotTo(HaveOccurred())
		hols, err := holidays.On(d, opts)
		Expect(err).NotTo(HaveOccurred())
		Expect(hasNamed(hols, "Каляды праваслаўныя")).To(BeTrue(), fmt.Sprintf("On(%s, %v): want holiday %q, got %v", s, opts.Regions, "Каляды праваслаўныя", hols))
	},
		Entry("2017-01-07", "2017-01-07"),
		Entry("2026-01-07", "2026-01-07"),
	)

	DescribeTable("003_МіжнародныЖаночыДзень", func(s string) {
		opts := holidays.Options{Regions: []string{"by"}}
		d, err := parseFlex(s)
		Expect(err).NotTo(HaveOccurred())
		hols, err := holidays.On(d, opts)
		Expect(err).NotTo(HaveOccurred())
		Expect(hasNamed(hols, "Міжнародны жаночы дзень")).To(BeTrue(), fmt.Sprintf("On(%s, %v): want holiday %q, got %v", s, opts.Regions, "Міжнародны жаночы дзень", hols))
	},
		Entry("2017-03-08", "2017-03-08"),
		Entry("2026-03-08", "2026-03-08"),
	)

	DescribeTable("004_СвятаПрацы", func(s string) {
		opts := holidays.Options{Regions: []string{"by"}}
		d, err := parseFlex(s)
		Expect(err).NotTo(HaveOccurred())
		hols, err := holidays.On(d, opts)
		Expect(err).NotTo(HaveOccurred())
		Expect(hasNamed(hols, "Свята працы")).To(BeTrue(), fmt.Sprintf("On(%s, %v): want holiday %q, got %v", s, opts.Regions, "Свята працы", hols))
	},
		Entry("2017-05-01", "2017-05-01"),
		Entry("2026-05-01", "2026-05-01"),
	)

	DescribeTable("005_ДзеньПерамогі", func(s string) {
		opts := holidays.Options{Regions: []string{"by"}}
		d, err := parseFlex(s)
		Expect(err).NotTo(HaveOccurred())
		hols, err := holidays.On(d, opts)
		Expect(err).NotTo(HaveOccurred())
		Expect(hasNamed(hols, "Дзень Перамогі")).To(BeTrue(), fmt.Sprintf("On(%s, %v): want holiday %q, got %v", s, opts.Regions, "Дзень Перамогі", hols))
	},
		Entry("2017-05-09", "2017-05-09"),
		Entry("2026-05-09", "2026-05-09"),
	)

	DescribeTable("006_ДзеньНезалежнасціРэспублікіБеларусьДзеньРэспублікі", func(s string) {
		opts := holidays.Options{Regions: []string{"by"}}
		d, err := parseFlex(s)
		Expect(err).NotTo(HaveOccurred())
		hols, err := holidays.On(d, opts)
		Expect(err).NotTo(HaveOccurred())
		Expect(hasNamed(hols, "Дзень Незалежнасці Рэспублікі Беларусь (Дзень Рэспублікі)")).To(BeTrue(), fmt.Sprintf("On(%s, %v): want holiday %q, got %v", s, opts.Regions, "Дзень Незалежнасці Рэспублікі Беларусь (Дзень Рэспублікі)", hols))
	},
		Entry("2017-07-03", "2017-07-03"),
		Entry("2026-07-03", "2026-07-03"),
	)

	DescribeTable("007_ДзеньКастрычніцкайРэвалюцыі", func(s string) {
		opts := holidays.Options{Regions: []string{"by"}}
		d, err := parseFlex(s)
		Expect(err).NotTo(HaveOccurred())
		hols, err := holidays.On(d, opts)
		Expect(err).NotTo(HaveOccurred())
		Expect(hasNamed(hols, "Дзень Кастрычніцкай рэвалюцыі")).To(BeTrue(), fmt.Sprintf("On(%s, %v): want holiday %q, got %v", s, opts.Regions, "Дзень Кастрычніцкай рэвалюцыі", hols))
	},
		Entry("2017-11-07", "2017-11-07"),
		Entry("2026-11-07", "2026-11-07"),
	)

	DescribeTable("008_КалядыКаталіцкія", func(s string) {
		opts := holidays.Options{Regions: []string{"by"}}
		d, err := parseFlex(s)
		Expect(err).NotTo(HaveOccurred())
		hols, err := holidays.On(d, opts)
		Expect(err).NotTo(HaveOccurred())
		Expect(hasNamed(hols, "Каляды каталіцкія")).To(BeTrue(), fmt.Sprintf("On(%s, %v): want holiday %q, got %v", s, opts.Regions, "Каляды каталіцкія", hols))
	},
		Entry("2017-12-25", "2017-12-25"),
		Entry("2026-12-25", "2026-12-25"),
	)

	DescribeTable("009_Радуніца", func(s string) {
		opts := holidays.Options{Regions: []string{"by"}}
		d, err := parseFlex(s)
		Expect(err).NotTo(HaveOccurred())
		hols, err := holidays.On(d, opts)
		Expect(err).NotTo(HaveOccurred())
		Expect(hasNamed(hols, "Радуніца")).To(BeTrue(), fmt.Sprintf("On(%s, %v): want holiday %q, got %v", s, opts.Regions, "Радуніца", hols))
	},
		Entry("2023-04-25", "2023-04-25"),
	)

	DescribeTable("010_Радуніца", func(s string) {
		opts := holidays.Options{Regions: []string{"by"}}
		d, err := parseFlex(s)
		Expect(err).NotTo(HaveOccurred())
		hols, err := holidays.On(d, opts)
		Expect(err).NotTo(HaveOccurred())
		Expect(hasNamed(hols, "Радуніца")).To(BeTrue(), fmt.Sprintf("On(%s, %v): want holiday %q, got %v", s, opts.Regions, "Радуніца", hols))
	},
		Entry("2024-05-14", "2024-05-14"),
	)

	DescribeTable("011_Радуніца", func(s string) {
		opts := holidays.Options{Regions: []string{"by"}}
		d, err := parseFlex(s)
		Expect(err).NotTo(HaveOccurred())
		hols, err := holidays.On(d, opts)
		Expect(err).NotTo(HaveOccurred())
		Expect(hasNamed(hols, "Радуніца")).To(BeTrue(), fmt.Sprintf("On(%s, %v): want holiday %q, got %v", s, opts.Regions, "Радуніца", hols))
	},
		Entry("2025-04-29", "2025-04-29"),
	)

	DescribeTable("012_Радуніца", func(s string) {
		opts := holidays.Options{Regions: []string{"by"}}
		d, err := parseFlex(s)
		Expect(err).NotTo(HaveOccurred())
		hols, err := holidays.On(d, opts)
		Expect(err).NotTo(HaveOccurred())
		Expect(hasNamed(hols, "Радуніца")).To(BeTrue(), fmt.Sprintf("On(%s, %v): want holiday %q, got %v", s, opts.Regions, "Радуніца", hols))
	},
		Entry("2026-04-21", "2026-04-21"),
	)

	DescribeTable("013_ДзеньАбаронцаўАйчыныІУзброеныхСілРэспублікіБеларусь", func(s string) {
		opts := holidays.Options{Regions: []string{"by"}, Informal: true}
		d, err := parseFlex(s)
		Expect(err).NotTo(HaveOccurred())
		hols, err := holidays.On(d, opts)
		Expect(err).NotTo(HaveOccurred())
		Expect(hasNamed(hols, "Дзень абаронцаў Айчыны і Узброеных Сіл Рэспублікі Беларусь")).To(BeTrue(), fmt.Sprintf("On(%s, %v): want holiday %q, got %v", s, opts.Regions, "Дзень абаронцаў Айчыны і Узброеных Сіл Рэспублікі Беларусь", hols))
	},
		Entry("2026-02-23", "2026-02-23"),
	)

	DescribeTable("014_ДзеньКанстытуцыі", func(s string) {
		opts := holidays.Options{Regions: []string{"by"}, Informal: true}
		d, err := parseFlex(s)
		Expect(err).NotTo(HaveOccurred())
		hols, err := holidays.On(d, opts)
		Expect(err).NotTo(HaveOccurred())
		Expect(hasNamed(hols, "Дзень Канстытуцыі")).To(BeTrue(), fmt.Sprintf("On(%s, %v): want holiday %q, got %v", s, opts.Regions, "Дзень Канстытуцыі", hols))
	},
		Entry("2026-03-15", "2026-03-15"),
	)

	DescribeTable("015_ДзеньЯднанняНародаўБеларусіІРасіі", func(s string) {
		opts := holidays.Options{Regions: []string{"by"}, Informal: true}
		d, err := parseFlex(s)
		Expect(err).NotTo(HaveOccurred())
		hols, err := holidays.On(d, opts)
		Expect(err).NotTo(HaveOccurred())
		Expect(hasNamed(hols, "Дзень яднання народаў Беларусі і Расіі")).To(BeTrue(), fmt.Sprintf("On(%s, %v): want holiday %q, got %v", s, opts.Regions, "Дзень яднання народаў Беларусі і Расіі", hols))
	},
		Entry("2026-04-02", "2026-04-02"),
	)

	DescribeTable("016_ДзеньНароднагаАдзінства", func(s string) {
		opts := holidays.Options{Regions: []string{"by"}, Informal: true}
		d, err := parseFlex(s)
		Expect(err).NotTo(HaveOccurred())
		hols, err := holidays.On(d, opts)
		Expect(err).NotTo(HaveOccurred())
		Expect(hasNamed(hols, "Дзень народнага адзінства")).To(BeTrue(), fmt.Sprintf("On(%s, %v): want holiday %q, got %v", s, opts.Regions, "Дзень народнага адзінства", hols))
	},
		Entry("2026-09-17", "2026-09-17"),
	)

	DescribeTable("017_Дзяды", func(s string) {
		opts := holidays.Options{Regions: []string{"by"}, Informal: true}
		d, err := parseFlex(s)
		Expect(err).NotTo(HaveOccurred())
		hols, err := holidays.On(d, opts)
		Expect(err).NotTo(HaveOccurred())
		Expect(hasNamed(hols, "Дзяды")).To(BeTrue(), fmt.Sprintf("On(%s, %v): want holiday %q, got %v", s, opts.Regions, "Дзяды", hols))
	},
		Entry("2026-11-02", "2026-11-02"),
	)

	DescribeTable("018_Negative", func(s string) {
		opts := holidays.Options{Regions: []string{"by"}}
		d, err := parseFlex(s)
		Expect(err).NotTo(HaveOccurred())
		hols, err := holidays.On(d, opts)
		Expect(err).NotTo(HaveOccurred())
		Expect(hols).To(BeEmpty(), fmt.Sprintf("On(%s, %v): expected no holiday, got %v", s, opts.Regions, hols))
	},
		Entry("2026-02-23", "2026-02-23"),
		Entry("2026-03-15", "2026-03-15"),
		Entry("2026-04-02", "2026-04-02"),
		Entry("2026-09-17", "2026-09-17"),
		Entry("2026-11-02", "2026-11-02"),
	)

})
