// Code generated by gen-holidays from definitions/cn.yaml@v9.0.0. DO NOT EDIT.

package definitions_test

import (
	"fmt"

	holidays "github.com/holidays/go-holidays"

	. "github.com/onsi/ginkgo/v2"
	. "github.com/onsi/gomega"
)

var _ = Describe("cn", func() {
	DescribeTable("000_元旦", func(s string) {
		opts := holidays.Options{Regions: []string{"cn"}}
		d, err := parseFlex(s)
		Expect(err).NotTo(HaveOccurred())
		hols, err := holidays.On(d, opts)
		Expect(err).NotTo(HaveOccurred())
		Expect(hasNamed(hols, "元旦")).To(BeTrue(), fmt.Sprintf("On(%s, %v): want holiday %q, got %v", s, opts.Regions, "元旦", hols))
	},
		Entry("2025-01-01", "2025-01-01"),
	)

	DescribeTable("001_除夕", func(s string) {
		opts := holidays.Options{Regions: []string{"cn"}}
		d, err := parseFlex(s)
		Expect(err).NotTo(HaveOccurred())
		hols, err := holidays.On(d, opts)
		Expect(err).NotTo(HaveOccurred())
		Expect(hasNamed(hols, "除夕")).To(BeTrue(), fmt.Sprintf("On(%s, %v): want holiday %q, got %v", s, opts.Regions, "除夕", hols))
	},
		Entry("2025-01-28", "2025-01-28"),
	)

	DescribeTable("002_春节", func(s string) {
		opts := holidays.Options{Regions: []string{"cn"}}
		d, err := parseFlex(s)
		Expect(err).NotTo(HaveOccurred())
		hols, err := holidays.On(d, opts)
		Expect(err).NotTo(HaveOccurred())
		Expect(hasNamed(hols, "春节")).To(BeTrue(), fmt.Sprintf("On(%s, %v): want holiday %q, got %v", s, opts.Regions, "春节", hols))
	},
		Entry("2025-01-29", "2025-01-29"),
	)

	DescribeTable("003_春节", func(s string) {
		opts := holidays.Options{Regions: []string{"cn"}}
		d, err := parseFlex(s)
		Expect(err).NotTo(HaveOccurred())
		hols, err := holidays.On(d, opts)
		Expect(err).NotTo(HaveOccurred())
		Expect(hasNamed(hols, "春节")).To(BeTrue(), fmt.Sprintf("On(%s, %v): want holiday %q, got %v", s, opts.Regions, "春节", hols))
	},
		Entry("2025-01-30", "2025-01-30"),
	)

	DescribeTable("004_春节", func(s string) {
		opts := holidays.Options{Regions: []string{"cn"}}
		d, err := parseFlex(s)
		Expect(err).NotTo(HaveOccurred())
		hols, err := holidays.On(d, opts)
		Expect(err).NotTo(HaveOccurred())
		Expect(hasNamed(hols, "春节")).To(BeTrue(), fmt.Sprintf("On(%s, %v): want holiday %q, got %v", s, opts.Regions, "春节", hols))
	},
		Entry("2025-01-31", "2025-01-31"),
	)

	DescribeTable("005_春节", func(s string) {
		opts := holidays.Options{Regions: []string{"cn"}}
		d, err := parseFlex(s)
		Expect(err).NotTo(HaveOccurred())
		hols, err := holidays.On(d, opts)
		Expect(err).NotTo(HaveOccurred())
		Expect(hasNamed(hols, "春节")).To(BeTrue(), fmt.Sprintf("On(%s, %v): want holiday %q, got %v", s, opts.Regions, "春节", hols))
	},
		Entry("2024-02-10", "2024-02-10"),
	)

	DescribeTable("006_Negative", func(s string) {
		opts := holidays.Options{Regions: []string{"cn"}}
		d, err := parseFlex(s)
		Expect(err).NotTo(HaveOccurred())
		hols, err := holidays.On(d, opts)
		Expect(err).NotTo(HaveOccurred())
		Expect(hols).To(BeEmpty(), fmt.Sprintf("On(%s, %v): expected no holiday, got %v", s, opts.Regions, hols))
	},
		Entry("2024-02-09", "2024-02-09"),
	)

	DescribeTable("007_除夕", func(s string) {
		opts := holidays.Options{Regions: []string{"cn"}}
		d, err := parseFlex(s)
		Expect(err).NotTo(HaveOccurred())
		hols, err := holidays.On(d, opts)
		Expect(err).NotTo(HaveOccurred())
		Expect(hasNamed(hols, "除夕")).To(BeTrue(), fmt.Sprintf("On(%s, %v): want holiday %q, got %v", s, opts.Regions, "除夕", hols))
	},
		Entry("2010-02-13", "2010-02-13"),
	)

	DescribeTable("008_Negative", func(s string) {
		opts := holidays.Options{Regions: []string{"cn"}}
		d, err := parseFlex(s)
		Expect(err).NotTo(HaveOccurred())
		hols, err := holidays.On(d, opts)
		Expect(err).NotTo(HaveOccurred())
		Expect(hols).To(BeEmpty(), fmt.Sprintf("On(%s, %v): expected no holiday, got %v", s, opts.Regions, hols))
	},
		Entry("2010-02-16", "2010-02-16"),
	)

	DescribeTable("009_清明节", func(s string) {
		opts := holidays.Options{Regions: []string{"cn"}}
		d, err := parseFlex(s)
		Expect(err).NotTo(HaveOccurred())
		hols, err := holidays.On(d, opts)
		Expect(err).NotTo(HaveOccurred())
		Expect(hasNamed(hols, "清明节")).To(BeTrue(), fmt.Sprintf("On(%s, %v): want holiday %q, got %v", s, opts.Regions, "清明节", hols))
	},
		Entry("2025-04-04", "2025-04-04"),
	)

	DescribeTable("010_清明节", func(s string) {
		opts := holidays.Options{Regions: []string{"cn"}}
		d, err := parseFlex(s)
		Expect(err).NotTo(HaveOccurred())
		hols, err := holidays.On(d, opts)
		Expect(err).NotTo(HaveOccurred())
		Expect(hasNamed(hols, "清明节")).To(BeTrue(), fmt.Sprintf("On(%s, %v): want holiday %q, got %v", s, opts.Regions, "清明节", hols))
	},
		Entry("2026-04-05", "2026-04-05"),
	)

	DescribeTable("011_劳动节", func(s string) {
		opts := holidays.Options{Regions: []string{"cn"}}
		d, err := parseFlex(s)
		Expect(err).NotTo(HaveOccurred())
		hols, err := holidays.On(d, opts)
		Expect(err).NotTo(HaveOccurred())
		Expect(hasNamed(hols, "劳动节")).To(BeTrue(), fmt.Sprintf("On(%s, %v): want holiday %q, got %v", s, opts.Regions, "劳动节", hols))
	},
		Entry("2025-05-01", "2025-05-01"),
	)

	DescribeTable("012_劳动节", func(s string) {
		opts := holidays.Options{Regions: []string{"cn"}}
		d, err := parseFlex(s)
		Expect(err).NotTo(HaveOccurred())
		hols, err := holidays.On(d, opts)
		Expect(err).NotTo(HaveOccurred())
		Expect(hasNamed(hols, "劳动节")).To(BeTrue(), fmt.Sprintf("On(%s, %v): want holiday %q, got %v", s, opts.Regions, "劳动节", hols))
	},
		Entry("2025-05-02", "2025-05-02"),
	)

	DescribeTable("013_Negative", func(s string) {
		opts := holidays.Options{Regions: []string{"cn"}}
		d, err := parseFlex(s)
		Expect(err).NotTo(HaveOccurred())
		hols, err := holidays.On(d, opts)
		Expect(err).NotTo(HaveOccurred())
		Expect(hols).To(BeEmpty(), fmt.Sprintf("On(%s, %v): expected no holiday, got %v", s, opts.Regions, hols))
	},
		Entry("2024-05-02", "2024-05-02"),
	)

	DescribeTable("014_端午节", func(s string) {
		opts := holidays.Options{Regions: []string{"cn"}}
		d, err := parseFlex(s)
		Expect(err).NotTo(HaveOccurred())
		hols, err := holidays.On(d, opts)
		Expect(err).NotTo(HaveOccurred())
		Expect(hasNamed(hols, "端午节")).To(BeTrue(), fmt.Sprintf("On(%s, %v): want holiday %q, got %v", s, opts.Regions, "端午节", hols))
	},
		Entry("2025-05-31", "2025-05-31"),
	)

	DescribeTable("015_端午节", func(s string) {
		opts := holidays.Options{Regions: []string{"cn"}}
		d, err := parseFlex(s)
		Expect(err).NotTo(HaveOccurred())
		hols, err := holidays.On(d, opts)
		Expect(err).NotTo(HaveOccurred())
		Expect(hasNamed(hols, "端午节")).To(BeTrue(), fmt.Sprintf("On(%s, %v): want holiday %q, got %v", s, opts.Regions, "端午节", hols))
	},
		Entry("2024-06-10", "2024-06-10"),
	)

	DescribeTable("016_中秋节", func(s string) {
		opts := holidays.Options{Regions: []string{"cn"}}
		d, err := parseFlex(s)
		Expect(err).NotTo(HaveOccurred())
		hols, err := holidays.On(d, opts)
		Expect(err).NotTo(HaveOccurred())
		Expect(hasNamed(hols, "中秋节")).To(BeTrue(), fmt.Sprintf("On(%s, %v): want holiday %q, got %v", s, opts.Regions, "中秋节", hols))
	},
		Entry("2024-09-17", "2024-09-17"),
	)

	DescribeTable("017_国庆节", func(s string) {
		opts := holidays.Options{Regions: []string{"cn"}}
		d, err := parseFlex(s)
		Expect(err).NotTo(HaveOccurred())
		hols, err := holidays.On(d, opts)
		Expect(err).NotTo(HaveOccurred())
		Expect(hasNamed(hols, "国庆节")).To(BeTrue(), fmt.Sprintf("On(%s, %v): want holiday %q, got %v", s, opts.Regions, "国庆节", hols))
	},
		Entry("2025-10-01", "2025-10-01"),
	)

	DescribeTable("018_国庆节", func(s string) {
		opts := holidays.Options{Regions: []string{"cn"}}
		d, err := parseFlex(s)
		Expect(err).NotTo(HaveOccurred())
		hols, err := holidays.On(d, opts)
		Expect(err).NotTo(HaveOccurred())
		Expect(hasNamed(hols, "国庆节")).To(BeTrue(), fmt.Sprintf("On(%s, %v): want holiday %q, got %v", s, opts.Regions, "国庆节", hols))
	},
		Entry("2025-10-02", "2025-10-02"),
	)

	DescribeTable("019_国庆节", func(s string) {
		opts := holidays.Options{Regions: []string{"cn"}}
		d, err := parseFlex(s)
		Expect(err).NotTo(HaveOccurred())
		hols, err := holidays.On(d, opts)
		Expect(err).NotTo(HaveOccurred())
		Expect(hasNamed(hols, "国庆节")).To(BeTrue(), fmt.Sprintf("On(%s, %v): want holiday %q, got %v", s, opts.Regions, "国庆节", hols))
	},
		Entry("2025-10-03", "2025-10-03"),
	)

})
