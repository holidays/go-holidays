# Binaries for programs and plugins
*.exe
*.exe~
*.dll
*.so
*.dylib

# Test binary, build with `go test -c`
*.test

# Output of the go coverage tool, specifically when used with LiteIDE
*.out

# Per-package coverage profiles written by `make test`
.cover.profile

# Build output
/bin/

# Beads / Dolt runtime files
.dolt/
*.db
.beads-credential-key

# Claude / beads orchestration — local only, never checked in
.claude/
.beads/
.worktrees/
CLAUDE.md
AGENTS.md
